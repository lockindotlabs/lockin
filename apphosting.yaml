runConfig:
  runtime: nodejs22

buildConfig:
  buildCommand: npx prisma generate --schema apps/api/prisma/schema.prisma && pnpm run build:web

env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
