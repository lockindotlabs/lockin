runConfig:
  runtime: nodejs22
  entrypoint: node --import tsx/esm apps/api/src/index.ts

buildConfig:
  buildCommand: pnpm run build:backend

env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
