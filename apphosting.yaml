runConfig:
  runtime: nodejs22
  entrypoint: node_modules/.bin/tsx apps/api/src/index.ts

buildConfig:
  buildCommand: pnpm run build:backend

env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
